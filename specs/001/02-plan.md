# 02 — Implementation Plan

> **Maintenance Invariant:** Sequenced work only. Design rationale lives in `wiki-llm/`; findings live in `00-validation.md`. Tasks are ordered so that a **working end-to-end version exists at the end of M1**, then widens.

**Priority: a working version first.** Every milestone ends with something runnable. Anything not required for that is tagged `later` in the wiki and does not get built.

## Progress

`✅` done · `☐` not started. Update the mark in the SAME commit that completes the task.

| Milestone                   |   Done |  Total |                 |
| :-------------------------- | -----: | -----: | :-------------- |
| M0 Foundation               |      8 |      8 | ████████        |
| M1 Walking skeleton         |     20 |     20 | ████████        |
| M3 Provider breadth         |      7 |      8 | ███████░        |
| M2 Concurrency & resilience |      7 |     10 | ██████░░        |
| M4 Escalation               |      0 |      6 | ░░░░░░░░        |
| M6 Cost reduction           |      7 |     10 | ██████░░        |
| M5 Hardening                |      — |      — | prose checklist |
| **Total**                   | **49** | **62** |                 |

**Reprioritized 2026-08-29 — M3 before M2.** Baya's value proposition is "a different CLI per task"; shipping on `codex` alone is an MVP of the engine, not of the product. Breadth now leads; concurrency and resume widen it afterward. Task ids are unchanged — only the execution order moved. See **Execution order** below.

**M6 revised 2026-08-30 — grouping replaced session reuse.** Session chain-collapse (`M6.4`) shipped, then was removed whole: **task grouping** does the same job for chains, does it for siblings too, works on all four adapters, and needs no session id, warm-cache window, resume verb, or cold-retry fallback. Net deletion. Design in `wiki-llm/execution.md` §Grouping / §Memory. Memory stays as the cross-group channel; its claude transcript reader was removed with it — undocumented path, one provider, so `files_changed` from the protocol result carries the cross-provider half instead.

**Next up:** M3 is landed bar `M3.4` (re-probe `copilot` — blocked on quota reset). All four adapters are registered and unit-covered; `codex` + `claude` run end to end, `opencode` + `copilot` await a local environment fix and are settled by `npm run test:contract`. Then **`M2.4` signal teardown** — the gate before calling this an MVP.

## Scope tiers

| In v1                                                                                                                                                                                                                                                                                                                                                         | Deferred (`later`)                                                                                                                                                |
| :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4 providers, JSON protocol, planner + validation + fallback, parallel scheduling, context bus (`link-only`/`truncate`), failure isolation, park & resume, SIGINT teardown, **one Baya per directory**, `doctor`, **layered config + first-run wizard**, **`ora` progress**, **`state.json` + `baya resume`/`runs` + recovery prompt**, `--plan-out/--plan-in` | worktree isolation · `summarize` context · plan cache · `--edit` · repair round-trip for malformed results · cold resume · adaptive rate-limit backoff · `gemini` |

---

## Execution order

The milestone sections below are numbered by theme, not by build order. Since the 2026-08-29 reprioritization the sequence is:

| Step | Task                                                                                                | Milestone | Why here                                                                                                                                                                                                                      |
| :--: | :-------------------------------------------------------------------------------------------------- | :-------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|  1   | **M2.6** result degradation ladder (rungs 2–3)                                                      | M2        | Hard prerequisite: `opencode` and `copilot` enforce no schema, so without `verbatim`/`fenced` extraction they cannot return a result at all. The `native` and `synthesized` rungs already exist in `src/providers/result.ts`. |
|  2   | **M3.3** claude adapter                                                                             | M3        | Verified surface; `claude` is on the reference machine and runs today. First real second provider — a genuine two-CLI run.                                                                                                    |
|  3   | **M3.1** opencode adapter                                                                           | M3        | Exercises the file-delivery path (`-f`), proving the abstraction against a second invocation shape before the unverified providers.                                                                                           |
|  4   | **M2.5** failure classifier                                                                         | M2        | Its done-criteria enumerate each provider's error shape; far easier to write with three adapters in hand than before them.                                                                                                    |
|  5   | **M3.4 + M3.5** copilot re-probe + adapter                                                          | M3        | Argv-only prompt, JSONL with `ephemeral` events filtered. Re-probe blocked on quota reset; adapter is written against the fake harness and finished when quota returns.                                                       |
|  6   | **M3.5b + M3.6** wizard model lists + model alias table                                             | M3        | `M3.6` is the model→provider routing (`sonnet`→`claude`, `gpt-*`→`codex`) — unknown alias errors with a suggestion, never a silent reassignment.                                                                              |
|  7   | **M3.7** contract tier                                                                              | M3        | `BAYA_CONTRACT=1`, excluded from CI. Passes locally against `codex` + `claude` now; `opencode`/`copilot` once their local environments are fixed.                                                                             |
|  8   | **M2.4** signal teardown                                                                            | M2        | Process-group teardown. Gate before calling anything an MVP — without it Ctrl-C orphans provider grandchildren.                                                                                                               |
|  9   | **M2.1 + M2.2** parallel scheduler + writer semaphore                                               | M2        | Performance. Sequential multi-provider already demonstrates the thesis.                                                                                                                                                       |
|  10  | **M2.3, M2.5b, M2.7, M2.8, M2.9** failure semantics, exhaustion, status UI, resume, recovery prompt | M2        | The widen phase. `M2.3` is nearly free — `markDescendantsSkipped` and exit codes already landed in M1.                                                                                                                        |

## M0 — Foundation _(no product behavior; unblocks everything)_

|  ✓  | #     | Task                                                                                                                                                                                                                        | Done when                                                                                                                        |
| :-: | :---- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------- |
| ✅  | M0.1  | Scaffold: `package.json` (`type: module`, `bin.baya`), `tsconfig` (strict, NodeNext), `.nvmrc` 24, eslint+prettier, `.gitignore` += `.baya/`, `dist/`, `node_modules/`                                                      | `npm run typecheck && npm run lint` clean                                                                                        |
| ✅  | M0.2  | **Jest + ESM spike — include `chalk` v6 (ESM-only), the usual detonator.** `@swc/jest`, `--experimental-vm-modules`, `.js`-extension `moduleNameMapper`                                                                     | A trivial ESM test importing a relative `./x.js` **and `chalk`** passes                                                          |
| ✅  | M0.3  | Custom lint rule banning `shell: true` and `exec`/`execSync`                                                                                                                                                                | Rule fails a fixture that uses them                                                                                              |
| ✅  | M0.4  | **`test/fixtures/fake-provider.mjs`** with the full knob set from `wiki-llm/testing.md`                                                                                                                                     | Replays a scripted scenario; honors `hang_ms`, `on_signal`, `spawn_child`, `expect_stdin`                                        |
| ✅  | M0.5  | **Structured logger** (`logging.md`): JSONL `trace` sink + filtered stderr renderer, `--log-level`, redaction **at the sink**, ANSI stripping                                                                               | Redacts `sk-`/`ghp_`-shaped strings; prompts elided to `prompt_bytes`; **nothing ever written to stdout**                        |
| ✅  | M0.5b | **Directory lock**: `O_EXCL` lockfile with pid + host + heartbeat, single-threshold stale detection, reclaim-with-warning, `inspectLock` for `doctor`. Non-blocking `acquire` — a conflicting Baya is an error, not a queue | Unit: second acquire refused; stale lock reclaimed; **live lock never broken** (verified cross-process against a real child pid) |
| ✅  | M0.6  | CI: typecheck, lint, test — fully offline                                                                                                                                                                                   | Green, no network                                                                                                                |
| ✅  | M0.7  | **`src/ui/theme.ts`** — the sole chalk importer; semantic tokens + glyphs; `--no-color`/`NO_COLOR`; forced level `0` for machine output. Lint rule bans chalk imports elsewhere                                             | Color-on and color-off snapshots both pass; Jest global setup forces `FORCE_COLOR=0`                                             |

> M0.2 and M0.4 are the two that hurt if deferred. The ESM spike surfaces late and badly; the fake provider is the only reason the rest of the system is testable at all.

## M1 — Walking skeleton _(the working version)_

**Goal:** on a fresh machine, `baya ./tasks.md` asks two setup questions once, stores the answer, then plans with `codex`, renders the DAG, confirms, runs two dependent tasks **sequentially**, writes artifacts, prints a report, exits `0`.

**Provider scope for M1 is `codex` alone** — strongest surface (file-in/file-out schema enforcement, `-C`, `--color never`), so the engine is built against the cleanest adapter. Breadth lands in M3, where each provider gets its own contract test.

|  ✓  | #      | Task                                                                                                                                                                                                                                                                                    | Done when                                                                                                                                                                            |
| :-: | :----- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅  | M1.1   | Zod schemas: manifest, `task_request`, `task_result` (**incl. `notes[]` with `info\|warn\|action_required`**), `ProviderEvent`. Emit `task_result.schema.json` to `.baya/schema/` at runtime                                                                                            | Round-trip unit tests per schema; `notes` valid on every status, empty array never null                                                                                              |
| ✅  | M1.2   | Manifest validation: id format/uniqueness, dep resolution, **cycle detection reporting the cycle path**, provider allowlist, `--max-tasks`                                                                                                                                              | Unit tests incl. self-loop, 3-cycle, dangling dep, dup id                                                                                                                            |
| ✅  | M1.3   | Graph: topo layering, ready-set, descendant marking _(pure)_                                                                                                                                                                                                                            | Unit tests incl. diamond and disconnected components                                                                                                                                 |
| ✅  | M1.4   | Provider registry + binary resolution chain + **`baya doctor`**                                                                                                                                                                                                                         | `doctor` reports path/version/capabilities; resolves `codex` on a machine where it is off `$PATH`                                                                                    |
| ✅  | M1.5   | **codex adapter**: `buildRun` (stdin delivery, `--json`, `--output-schema <file>`, `-o <file>`, `-s`, `-C`, `--color never`), event map (`thread.started`→`thread_id`, `item.completed`→text, `turn.completed`→usage), `extractResult` from the `-o` file                               | argv **snapshot test**; asserts `-p` is _not_ used as prompt; **confirm `thread_id` is what `exec resume` accepts**                                                                  |
| ✅  | M1.5b  | **stdin discipline**: every spawn sets `stdin` to pipe or `/dev/null`, never inherit                                                                                                                                                                                                    | Test asserts no 3 s stall and no stdin warning on an argv-delivered prompt                                                                                                           |
| ✅  | M1.6   | Planner: markdown → codex → manifest; **1 repair round**; deterministic linear fallback                                                                                                                                                                                                 | Fake provider returns cycle → dangling → valid; and 3× garbage → linear fallback with warning                                                                                        |
| ✅  | M1.7   | Sequential executor: spawn `detached`, capture streams, persist `request/result/output/events/stdout/stderr`                                                                                                                                                                            | Integration: 2-task chain against fake provider, all artifacts present                                                                                                               |
| ✅  | M1.7b  | **`state.json`**: full schema (`recovery.md`), atomic write **before** each transition, `config_snapshot`, `totals`, per-task timings/cost/session id. `runId = <utc-ts>-<rand>-<pid>`                                                                                                  | Kill mid-run ⇒ valid JSON reflecting the last transition                                                                                                                             |
| ✅  | M1.7h  | **Acquire `.baya/baya.lock` at startup** for `run`/`resume`; refuse a second Baya with pid, runId, and age; release on exit                                                                                                                                                             | Integration: second invocation in the same directory exits `2` with the holder's details; a stale lock from a killed Baya is reclaimed                                               |
| ✅  | M1.7d  | **Full event vocabulary emitted** (`logging.md`) — every startup, planning, scheduling, execution, and teardown event                                                                                                                                                                   | Integration asserts a 2-task run emits the expected event sequence in `baya.jsonl`, `source.read` → `run.completed`                                                                  |
| ✅  | M1.7c  | **`ora` progress** via `src/ui/progress.ts`: stderr-only, auto-off for non-TTY/`--json`/`NO_COLOR`/`--no-progress`, **cursor restored on every exit path**                                                                                                                              | Test asserts `--json` stdout parses with progress on; SIGINT test asserts cursor-restore escape emitted                                                                              |
| ✅  | M1.7g  | **Bubble provider output up as `info`**: forward `text`/`tool`/child-stderr to the main process, ANSI-stripped, **task-prefixed**, rendered through `progress.ts`; `session`/`unknown` at `debug`; `--log-level`/`--quiet`/`--verbose` filter display only                              | Integration: fake provider emitting prose + tool calls + stderr appears on stderr prefixed; **`baya.jsonl` still holds the full stream at every level**; `--json` stdout stays clean |
| ✅  | M1.7e  | **Surface task output** (`cli.md` §Run output): per-task completion line = `summary` first line ≤120 chars; `warn`/`action_required` notes printed **immediately** on completion; `info` held for the report; full `output` printed only for single-task runs or `--verbose`; `--quiet` | Integration: a fake provider returning `notes` at each severity produces the right immediate-vs-deferred split                                                                       |
| ✅  | M1.7f  | **End-of-run report** with aggregated **Flagged** section (`action_required` first), totals, cost, output paths; `--json` carries per-task `notes` + aggregated `flagged`                                                                                                               | Snapshot (color off); section omitted entirely when no notes; **nothing terminal-only is missing from `--json`**                                                                     |
| ✅  | M1.8   | Context bus v1: `link-only` + `truncate`, per-edge/total budgets                                                                                                                                                                                                                        | Unit: 200 KB upstream ⇒ `inline: null`, valid `output_path`                                                                                                                          |
| ✅  | M1.9   | CLI: **bare-path form** (`baya ./tasks.md` ≡ `run`), `--dry-run`, `--yes`, `--plan-out/--plan-in`, DAG render, summary report, exit codes                                                                                                                                               | Subcommand-vs-path resolution unit tests; plan round-trip integration                                                                                                                |
| ✅  | M1.10a | **Config layer**: precedence (flags > env > project > user > built-in), atomic writes, source tracking; `baya config --show \| path \| set`                                                                                                                                             | Unit tests per precedence layer; `--show` names the source of each value; malformed config errors clearly, never silently resets                                                     |
| ✅  | M1.10b | **First-run wizard** (`@inquirer/prompts`): pure `buildProviderChoices` / `buildModelChoices`, dynamic list via `opencode models` (searchable), trigger + bypass rules, **non-TTY fallbacks that never hang**, then continue the original command                                       | Pure choice-builders unit-tested; **no test opens a prompt**; integration covers non-TTY with one / several / zero providers                                                         |
| ✅  | M1.10  | **`-h/--help`**: provider list generated from the registry with resolution status (colored via `theme`), plus runnable examples                                                                                                                                                         | Snapshot test (color off); adding a fake adapter changes the snapshot with no other edit                                                                                             |

**Exit criteria:** a real `tasks.md` with a dependency executes end to end on real `codex`; the same flow passes offline against fake providers.

**Status — landed.** The offline half is covered by `test/integration/`: a two-task chain, the full event sequence, failure isolation, the directory lock, the plan round-trip, and stdin discipline. Two things M1 deliberately did **not** settle, both carried forward with their owners:

- `codex exec resume <thread_id>` remains **UNVERIFIED**. `buildResume` assumes `thread_id` is the identifier `resume` accepts, and nothing in M1 exercises resume. Settled by the contract tier (M3.7); needed by M4.
- A `needs_input` result **parks** the task and stops its branch. The prompt queue, the answer, and the session resume are M4.

## M2 — Concurrency & resilience

> **Build order:** M2 is interleaved with M3 — see **Execution order**. `M2.6` leads (it unblocks the non-schema adapters), `M2.5` follows the adapters, `M2.4` gates the MVP, and the rest is the widen phase after M3.

|  ✓  | #     | Task                                                                                                                                                                                                                                                                                                                                                                                            | Done when                                                                                                                                              |
| :-: | :---- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅  | M2.1  | Parallel scheduler: global + per-provider budgets, `--max-parallel`                                                                                                                                                                                                                                                                                                                             | Integration: fan-out/fan-in; concurrency never exceeds budgets                                                                                         |
| ✅  | M2.2  | Single-writer serialization via an **in-memory scheduler semaphore** (no file lock — one Baya per directory); readers parallel                                                                                                                                                                                                                                                                  | Integration: two independent `writes: true` tasks never overlap; two readers do                                                                        |
|  ☐  | M2.3  | Failure semantics: descendants ⇒ `skipped`, `--on-error`, exit `1`                                                                                                                                                                                                                                                                                                                              | Integration: mid-graph failure; independent branch still succeeds                                                                                      |
| ✅  | M2.4  | **Signal teardown**: process groups, SIGTERM → 5 s → SIGKILL, double-Ctrl-C, `uncaughtException`                                                                                                                                                                                                                                                                                                | Integration: fakes spawning grandchildren; assert **zero surviving pids via `ps`**, exit `130`                                                         |
| ✅  | M2.5  | **Failure classifier** → normalized `failure.kind` + `retry: now\|later\|never` from real provider signals; timeouts; retries for `now` only                                                                                                                                                                                                                                                    | Unit: copilot `quota_exceeded`/402, opencode `isRetryable`/401, claude `permission_denials`, timeout, schema. **`quota` consumes zero retry attempts** |
| ✅  | M2.5b | **Provider exhaustion**: a `quota` failure halts the **whole run** — stop admitting, drain what is in flight, report; run stays resumable. _Revised 2026-08-31: was "that provider only, other branches continue". A quota wall usually means the session or the account is done, and finishing half a graph elsewhere spends money to land somewhere no more resumable than stopping cleanly._ | Integration: two providers, one quota-fails; nothing further spawns on either; in-flight work finishes; never-started tasks `skipped` and resumable    |
| ✅  | M2.6  | Result degradation ladder (native → verbatim → fenced → synthesized failure)                                                                                                                                                                                                                                                                                                                    | Unit: prose-wrapped, fenced, garbage each land correctly                                                                                               |
|  ☐  | M2.7  | Live status UI + `--json` report + `--verbose` event stream, all through `theme`                                                                                                                                                                                                                                                                                                                | Snapshot of report JSON; **`--json` parses cleanly with color forced on**; ANSI stripped from provider output                                          |

| ✅ | M2.8 | **`baya resume <runId>` / `baya runs`**: re-run `failed`/`skipped`/`parked`/interrupted, keep `succeeded` outputs as context, `--provider` override, stale-source (`sha256`) guard, `--yes` non-interactive path. **No `runId` ⇒ picker, never an implicit choice**; non-TTY ⇒ exit `2` | Integration: interrupt → resume → completes; **no succeeded task re-runs**; stale source warns; second concurrent resume refused |
| ☐ | M2.9 | **Recovery prompt** (`@inquirer/prompts`): run summary + failure detail + log path; **options ordered by `failure.kind`** — "different provider" first for `quota`/`auth`, plain retry first for `now` kinds | Pure `buildRecoveryChoices(state)` unit-tested per kind; no test opens a prompt |

> **Concurrency is closed — decided 2026-08-31.** `M2.1` + `M2.2` landed and that is the whole
> concurrency story for v1: **independent `read-only` tasks run at once; every `read-write` task
> takes the single writer key and runs alone.** Parallel writers are not a missing flag, they are a
> different feature — see below.
>
> Why writers do not parallelize: they share one working tree. Two agents editing it at once do not
> usually collide on a file — they collide on the build. One runs the test suite while the other is
> mid-edit and gets a failure that means nothing, costs real money, and does not reproduce. Also
> `.git/index.lock`, `tsbuildinfo`, and every shared cache. `depends_on` cannot express this: the
> interference is in the filesystem, not the graph. And there is nothing to fail fast on — Baya
> cannot know what a provider will touch until it has touched it.
>
> Rejected: an `--isolation none` opt-out. It pays off as flaky failures, not as speed.
>
> **The one remaining concurrency item is worktree isolation** (`later`, own milestone — not M2).
> Real parallel writers means one working tree per writer, and its cost is not `git worktree add`:
> it is merging N branches back into the user's tree, and giving each tree its own `node_modules`.
> The merge-back is a design question and must be settled before any code.

## M3 — Provider breadth

> **Current focus.** Runs ahead of most of M2 — see **Execution order**. `M2.6` is a prerequisite and comes first.

|  ✓  | #     | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Done when                                                                                                      |
| :-: | :---- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------- |
| ✅  | M3.1  | **opencode adapter** — validates the abstraction: native `-f` **file** delivery vs codex's stdin, `-m provider/model`, `--format json`, `-s <session>`                                                                                                                                                                                                                                                                                                                                                                                                                                                    | argv snapshot; delivery-preference chain covered                                                               |
|  ☐  | M3.2  | ~~Probe `claude`~~ **done 2026-08-28** — surface, events, and `--json-schema` behavior recorded in `providers.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ✅                                                                                                             |
| ✅  | M3.3  | **claude adapter**: `-p` + stdin, `--json-schema` **inline** (file path is rejected), read `.structured_output`, **`cwd` on the spawn — no cwd flag exists**, `--session-id` pre-assign, `--permission-mode` map                                                                                                                                                                                                                                                                                                                                                                                          | argv snapshot + contract test; test asserts a file path is never passed to `--json-schema`                     |
|  ☐  | M3.4  | ~~Install and probe `copilot`~~ **partially done 2026-08-28** — flags + JSONL confirmed; **re-probe the success path once monthly quota resets**                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Assistant-text event shape recorded                                                                            |
| ✅  | M3.5  | **copilot adapter**: `-p <text>` **argv-only**, `--output-format json` JSONL with `ephemeral` events **filtered**, `result` event → session id + exit code + `usage.codeChanges.filesModified`, `--no-ask-user`, `--no-color`, `--session-id` pre-assign                                                                                                                                                                                                                                                                                                                                                  | argv snapshot + contract test; `files_changed` populated from the `result` event                               |
| ✅  | M3.5b | Fill each provider's curated model list in the wizard as its adapter lands; `opencode` uses live `opencode models`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Wizard shows a usable list per integrated provider                                                             |
| ✅  | M3.6  | **Model catalog + resolution.** Hardcoded `{id,aliases,description}` lists for the 3 CLIs with no list command (`src/providers/catalog.ts`), live `opencode models`, cached to `modelCatalog` at setup + `baya config refresh-models`. Every run resolves a task-named model (user alias → exact → best-match) at a **model gate** before the plan gate; no confident hit ⇒ prompt (best / provider default / exit), `--yes` accepts ≥ 0.85 else exit 2. `modelAliases` user map. **A named model never silently becomes the default.** `providerForModel` pattern-routes a literal id not in the catalog | Unit: catalog resolve, scoreModel typo, model-gate abort-not-default, alias merge, `config set modelAliases.x` |
| ✅  | M3.7  | Contract tier behind `BAYA_CONTRACT=1`, excluded from CI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `npm run test:contract` passes locally against all four                                                        |

> M3.1 before M3.2–M3.5 deliberately: `opencode` is verified _and_ exercises the file-delivery path, so the abstraction is proven against two shapes before the unverified providers arrive.

## M4 — Escalation (Pause & Resume)

|  ✓  | #     | Task                                                                                                                                                                       | Done when                                                                            |
| :-: | :---- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------- |
|  ☐  | M4.1  | Session ids: **pre-assign** a UUID for `claude`/`copilot` (`--session-id`); **capture** for `codex` (`thread_id`) / `opencode` (`sessionID`). Normalize all four spellings | Integration asserts both paths; resume works without parsing for the pre-assign pair |
|  ☐  | M4.1b | Disable provider-native question tools (`copilot --no-ask-user`) so questions return as `needs_input` JSON rather than blocking                                            | Test asserts the flag is present for copilot                                         |
|  ☐  | M4.2  | Park queue: `needs_input` ⇒ `parked`, **independent branches keep running**                                                                                                | Integration: parked node does not stall a sibling                                    |
|  ☐  | M4.3  | Serialized stdin ownership + question rendering (task id, provider, title, options)                                                                                        | Two simultaneous questions bubble one at a time                                      |
|  ☐  | M4.4  | Session resume dispatch per adapter (`codex exec resume`, `opencode run -s`, `claude --resume`) with the answer in context                                                 | Integration: park → answer → resume → `0`                                            |
|  ☐  | M4.5  | `--on-input ask\|fail\|skip\|default`; **non-TTY fails cleanly, never hangs**                                                                                              | Integration with `stdin` not a TTY                                                   |

## M6 — Cost reduction _(task grouping + cross-task memory)_

**Why:** per-task model routing already saves money; the next-largest waste is that every task pays for its own **process**, and so starts blind. Measured on 23 recorded runs in `.baya/runs/`: `wiki-llm/index.md` was independently re-read by 7 tasks, `package.json` by 6, `npm run typecheck` re-run by 4 — and `npm test` failed then `npm test -- --runInBand` succeeded, rediscovered task after task.

Design and rationale: [`wiki-llm/execution.md`](../../wiki-llm/execution.md) §Grouping and §Memory. Grouping needs nothing from an adapter and covers all four; command observations are scoped to `codex`, and `files_changed` covers everyone.

|  ✓  | #     | Task                                                                                                                                                                                                                                                        | Done when                                                                                                                |
| :-: | :---- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------- |
| ✅  | M6.1  | **codex `file_change` fix** — the field is `changes: [{path, kind}]`, not `path`; every file change rendered as a bare `Edit()`                                                                                                                             | Unit test on the real shape; `files_changed` facts are non-empty                                                         |
| ✅  | M6.2  | **`observations` capability** — codex from its own documented `events.jsonl`; every provider's `files_changed` from the protocol result. The claude transcript reader was built and **removed**: undocumented path, one provider only                       | Unit test per adapter; `observations: "none"` consumes memory and contributes none                                       |
| ✅  | M6.3  | **`src/memory/`** — pure `deriveMemory` + `renderMemory`, one prompt section by `# Workspace`, `runs/<id>/memory.json`, `--no-memory` / `--memory-budget`                                                                                                   | Delivery proven by `expect_stdin` in `test/integration/memory.test.ts`; per-kind caps stop one flailing task dominating  |
| ✅  | M6.4  | **~~Chain-collapse by session resume~~ — shipped, then removed.** Superseded by M6.8                                                                                                                                                                        | Removed whole: `buildContinue`, `transcriptPath`, `continued_from`, `--no-session-reuse`                                 |
| ✅  | M6.8  | **Task grouping** — group any tasks sharing `(provider, model, access, cwd)` whose deps are `succeeded` or in the group; one process, `task_result_batch` back, `--group-size` (default 4)                                                                  | `group_id` in `state.json`; chains, siblings, the cap and the `access` boundary each asserted; `--group-size 1` bypasses |
| ✅  | M6.7  | **Rename `writes` → `access`** (`"read-only" \| "read-write"`). The flag governs what a task may **do**, not what it edits; a boolean named for an effect could not carry that. Clean break — the manifest is versioned and nothing is published against it | A stale plan file carrying `writes` exits `2` rather than defaulting; plan preview badges only `read-write`              |
| ✅  | M6.9  | **A denied command is not a dead end** — drop failed-command facts from any task that reported `permission_denials`                                                                                                                                         | Regression test on the real shape; successes and file facts survive a denial                                             |
|  ☐  | M6.10 | **`--permission-mode auto` denies Bash under `-p`.** Pre-existing, all models, 4–14 denials per task. Candidates: `--allowedTools Bash` for `read-write`, or `--permission-mode dontAsk` — **neither live-verified, so neither shipped**                    | One `claude -p` probe per candidate settles it; the fix is whichever actually runs `npm test`                            |
|  ☐  | M6.6  | **Measure it.** Report exploration tool-calls per task with and without `--no-memory`; token variance is too high to read the effect directly                                                                                                               | A run report carries the comparison; the README claim is backed by numbers rather than by design intent                  |

> `M6.6` before advertising this. The whole feature is a bet that memory costs less than the rediscovery it prevents, and `--no-memory` exists precisely so the bet is checkable.

## M5 — Hardening & release

`README.md` (quickstart + wiki pointer) · `.baya/config.json` overrides · **`baya doctor` stray-pid reaping — gated on a stale `.baya/baya.lock`, never on pid-liveness alone**; reports unparseable locks for manual deletion · provider version-drift warning · npm packaging and `baya` bin smoke test · full contract-tier pass.

---

## Risk register

| Risk | Mitigation | Milestone |
| :-: | :-- | :-- | :-- |
| `copilot` surface unknown; may have no structured output | Probe before designing; `text` + fenced-extract fallback already in the ladder | M3.4 |
| `claude` off `$PATH`, surface unprobed | Resolution chain + `doctor` land in M1.4, before the adapter | M1.4, M3.2 |
| Provider flag drift breaks adapters silently | argv snapshots + contract tier + version-drift warning | M1.5, M3.7 |
| Jest + ESM friction discovered late | Spike it in M0.2 | M0.2 |
| Subscription throttling under parallelism | Conservative per-provider caps (`claude`/`copilot` = 1), config-raisable | M2.1 |
| Planner unreliability | Schema + repair round + deterministic linear fallback; **never abort on a bad plan** | M1.6 |
| Orphaned grandchildren after Ctrl+C | Process groups; test asserts via `ps`, not promise resolution | M2.4 |
| Abstraction over-fits codex | opencode adapter first in M3, before the unverified providers | M3.1 |
| ANSI leaks into `--json`/artifacts, breaking `jq` pipelines | Force color level `0` on machine paths; regression test with color forced on | M0.7, M2.7 |
| copilot success path never validated (quota exhausted 2026-08-28) | Fake-provider tests cover the engine; re-probe and finish `providers.md` when quota resets | M3.4 |
| opencode unusable locally — invalid OpenAI key (`"asd"`) in local config | **Environment fix, not code.** Repair auth before the M3.1 contract test | M3.1 |
| Inherited stdin stalls `claude -p` 3 s per task (~1 min over 20 tasks) | Always set stdin explicitly | M1.5b |
| **`ora` is single-line and cannot multiplex** — breaks when M2 adds parallelism | M1 is sequential so ora fits; at M2 switch to one aggregate line or `log-update`. Never run N ora instances | M1.7c, M2.7 |
| Hard exit leaves the user's terminal with a hidden cursor (ora) | Restore cursor in every signal handler, not just the happy path | M1.7c, M2.4 |
| Agent flags a real risk ("done, but this locks the table") and it dies in `result.json` unread | First-class `notes[]` in the schema; `warn`/`action_required` printed immediately; **Flagged** section closes every run | M1.1, M1.7e, M1.7f |
| Writing to stderr while ora spins garbles the line | All persistent output goes through `src/ui/progress.ts` | M1.7c, M1.7e |
| Streamed provider output from N parallel tasks interleaves into noise | Mandatory task prefix on every forwarded line; `--quiet` to suppress; M1 is sequential so this first lands in the easy case | M1.7g, M2.7 |
| Chatty providers flood the terminal (large file reads, long tool inputs) | Display abbreviates tool inputs to one line; **files are never truncated** | M1.7g |
| Resuming a run whose `tasks.md` changed silently executes a stale plan | `source.sha256` guard: warn, offer re-plan / continue / abort | M2.8 |
| Two processes resume the same run and double-spend credits | Subsumed: a second Baya in the directory cannot start at all | M1.7h |
| **`baya doctor` reaps a running Baya's children** | Reap only when `.baya/baya.lock` is **stale** (pid gone **and** heartbeat aged). Never on pid-liveness alone; when in doubt, report and leave it | M5 |
| Two Bayas in one tree corrupt each other's work | Refused at startup by the directory lock | M1.7h |
| A crashed Baya's lock wedges the directory forever | Stale reclaim (heartbeat aged + pid gone). Pid-reuse remains a known manual-delete case, reported by `doctor` | M0.5b |
| Logs leak prompts (source code) into pasted issue reports | Redact at the sink; `argv` prompt elided to `prompt_bytes` + pointer to `request.json` | M0.5 |
| Retrying an exhausted quota wastes time and never succeeds | `retry: "later"` — `quota`/`auth` consume no attempts; resume (optionally elsewhere) instead | M2.5 |
| copilot is argv-only for prompts, breaking the "prefer files" rule | Delivery-preference chain already degrades per adapter; document the exception | M3.5 |
| Wizard hangs a piped/CI invocation | Explicit non-TTY fallbacks; `BAYA_NO_INPUT=1` and `--default-provider` bypass; no test may open a prompt | M1.10b |
| No CLI validates model names cheaply (`codex` 400s, `claude` reports `unrecognized_model`, only `opencode` enumerates) | Hardcoded catalog for the three that can't list + live `opencode models`, cached to `modelCatalog` at setup. Resolution is offline string-matching against it — **no per-run validation call**. A name that won't resolve stops at the model gate | M3.6 |
| Memory carries a wrong fact into every later task | Derived only — never model-reported; keyed so a later fact replaces an earlier one; never carries command output. A command that failed then succeeded stops being a dead end | M6.3 |
| An observation source depends on a file nobody documents | The claude transcript reader was removed for exactly this. A new source must be documented by the provider and offerable by more than one — `files_changed` is the model | M6.2 |
| A denied command is recorded as a dead end and every later task refuses to try it | **Happened, not hypothetical:** `auto` denied `npm test`, memory filed it as failed, and the run cascaded. A task reporting `permission_denials` contributes no failed-command facts | M6.9 |
| `--permission-mode auto` denies Bash under `-p` on every model | **Pre-existing and widespread** — 4–14 denials per task across sonnet/opus/haiku runs. `withDenialNote` surfaces it, memory made it fatal until M6.9. Candidates are `--allowedTools Bash` for `read-write` and `--permission-mode dontAsk`; neither is live-verified, so neither is shipped | M6.10 |
| Memory becomes an injection channel into every task | Command strings and paths only, never `aggregated_output`/`tool_result.content`; rendered as evidence, not instruction | M6.3 |
| Grouping silently fights per-task model routing | `model` is in the grouping key, so a group partitions at every model change — routing wins by construction | M6.8 |
| A group widens a read-only task's permissions | `access` is in the grouping key. One process, one sandbox: a read-only task can never share it with a read-write one | M6.8 |
| One long prompt makes the model conflate or skip tasks | `--group-size` caps it (default 4), each task gets its own contract, and the response is one entry per task keyed by `task_id`. `--group-size 1` opts out entirely | M6.8 |
| A group's results get filed under the wrong task | Matched by `task_id`, **never** by position. A task the provider did not name is reported failed rather than credited with another's output | M6.8 |
| A model reports a task done whose dependency failed | The DAG decides, not the model: a member downstream of a failed member is `skipped` whatever it claimed. Guard is `markDescendantsSkipped(id, inGroup)` — the `pending`-only version passed over `running` group members | M6.8 |
| A group that dies partway loses tasks that had finished | Results are read per task, so finished work is banked; the first casualty is `failed` and everything after it `skipped`. The cap bounds what is at stake in any one process | M6.8 |
| Solving a cost problem by adding session management again | Grouping already covers chains **and** siblings, on every adapter, with no session id or resume verb. `wiki-llm/execution.md` §Grouping says so explicitly | M6.8 |
| `writes` read as "this edits my source" and alarmed users about tasks that only ran a test | Renamed to `access: "read-only" \| "read-write"` — an enum named for the permission, not a boolean named for an effect. Clean break; a stale `--plan-in` file is refused, never silently defaulted | M6.7 |
| Provider ANSI escapes reach the terminal unsanitized | `--color never` at the flag level + strip residual ANSI before render/persist | M0.5, M2.7 |
